# Root-level reference only -- each service also has its own .env.example
# (backend/.env.example, frontend/.env.example) with service-specific values.

DATABASE_URL=postgresql://tuneforge:tuneforge@localhost:5432/tuneforge
