# Frontend

React + TypeScript + Tailwind app, deployed on Vercel.

Generated from a v0.dev prompt (see `docs/` in the repo root or the project notes) and then wired up to the real API.

## Setup

```bash
npm install
cp .env.example .env   # set VITE_API_BASE_URL
npm run dev
```

Replace this README with real content once the generated app is dropped in here.
